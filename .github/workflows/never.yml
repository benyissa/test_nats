name: Never executed

on:
  workflow_run:
    workflows:
      - I will fail
    types:
      - completed

jobs:
    never_executed:
        if: ${{ github.event.workflow_run.conclusion == 'success' }}
        runs-on: ubuntu-latest
        steps:
            - name: ok
              run: echo "I will never execute"
