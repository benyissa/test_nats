name: Never executed

on:
  workflow_run:
    workflows:
      - I will fail
    types:
      - completed

jobs:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    bad:
        runs-on: ubuntu-latest
        steps:
            - name: ok
              run: echo "I will never execute"
